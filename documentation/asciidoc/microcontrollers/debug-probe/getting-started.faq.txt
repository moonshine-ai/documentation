# Getting started

How do I connect a Debug Probe to a Pico H?

What's the difference between the "D" port and the "U" port on the Debug Probe?

I've got a regular Pico, not the H version — can I still use the Debug Probe with it?

My Pico doesn't have a JST-SH connector. How do I hook it up to the Debug Probe?

Which wire goes to SWCLK? I've got the JST-SH to header cable and I'm not sure which color is which.

So RX goes to TX and TX goes to RX? That seems backwards — is that right?

I have a Pico W, not a Pico H. Do I need to do anything differently to connect the Debug Probe?

What pins do I need to solder on a regular Pico to get it working with the Debug Probe?

# Whole Document

What even is a Debug Probe and why would I need one for my Pico?

I just got a Debug Probe in the box with my Pico H — what do I do with it?

Do I need to solder anything if I have a Pico H, or does it just plug in?

What cables come with the Debug Probe? I want to make sure I have everything before I start.
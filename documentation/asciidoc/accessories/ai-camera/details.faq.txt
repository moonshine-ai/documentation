# Whole Document

- Wait, so the AI processing is actually happening inside the camera itself? Not on the Pi?
- I'm confused about what a tensor even is. Can you give me a simple explanation?
- What's the difference between the input tensor and the output tensor?
- So do I need to write code to use the AI camera or can I just point and shoot?
- What programming languages can I use to work with the AI camera?

# Overview

- Why doesn't the Raspberry Pi have to do any AI processing when using this camera?
- What's a Region of Interest and why would I want to change it?
- By default, does the camera use the whole image for AI processing or just part of it?
- Can I actually see the input tensor, like for debugging or whatever?
- So what even is an ISP? I keep seeing that term.
- I want to focus the AI on just one part of the image, like not the whole frame. Is that possible?

# System Architecture

- When the camera is streaming, does it send me images AND AI results at the same time?
- What exactly gets loaded onto the camera when it first starts up?

# Device Drivers

- Why does it take so long to load the neural network onto the camera the first time?
- I've loaded a model before and now it seems faster. Is the camera remembering it somehow?
- What's the RP2040 doing in all this? I thought that was a microcontroller for other stuff.
- Does the AI camera work differently on a Pi 5 versus older Pis?

# `libcamera`

- How do I actually get the AI results out in my code?
- What's in the output tensor data that comes back from the camera?
- Is there a way to get the input tensor too, or just the output?
- What does `CnnOutputTensorInfo` actually tell me about my model?

# `rpicam-apps`

- I want to write my own AI processing stage for a custom model. Where do I even start?
- How do I get the output tensor data inside my C++ code for each frame?
- What happens after I've processed the output tensor — how do I pass results to the next stage?
- What does `ConvertInferenceCoordinates` do and why would I need it?
- There's a function that shows a progress bar while the firmware loads — how do I use that?
- How do I set the region of interest from my C++ code?

# Picamera2

- How do I use the AI camera in Python?
- I'm using Python and I want to get the output tensors. How do I do that?
- I'm looking at the object detection demo in Python. How does it draw the bounding boxes on screen?
- Does the Python version have the same features as the C++ version?
- I want to know how confident the AI is about each detection. Can I get that score in Python?
- How do I convert the coordinates from the AI result to match where they actually are on the final image?
- Is there any way to check how well the camera's AI is performing, like frame-level stats or something?
- I want to set the region of interest in my Python script. What function do I call and what format does it need?
- How do I get the size of the input tensor for my neural network in Python?
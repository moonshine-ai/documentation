# External Trigger on the Global Shutter Camera

What's the XTR connection on the Global Shutter camera for?

How do I sync two Global Shutter cameras together?

So how long is the actual exposure time when I send a pulse? Like if I pulse it for 10 milliseconds, what do I get?

I want 30 frames per second — how fast do I need to pulse the pin?

I've got the GS camera and I want to use external triggering, but I'm not sure where to even start. What's the basic setup?

# Preparation

There's a transistor on my board labeled Q2 — do I need to remove anything before I can use external trigger mode?

Why do I need to remove R11? What happens if I leave it on there?

XTR says it's a 1.8 volt input but my Pi puts out 3.3 volts. How do I deal with that?

Can I use a Pico to trigger the Global Shutter camera? How do I wire that up?

What resistors do I need when connecting the Pico to the camera's XTR pin?

I want to set a specific shutter speed and framerate with the Pico. Is there some code I can use for that?

So the MicroPython code — what does the shutter value actually mean? Is that in milliseconds or what?

What pin on the Pico connects to XTR in the example?

I'm nervous about the soldering part — is this something a beginner should try?

# Camera driver configuration

Do I have to mess with the config.txt file, or is that only sometimes needed?

I've got two GS cameras wired to the same trigger. What do I need to change in config.txt?

I'm using the CAM0 port on a Pi 5 — is there anything different I need to add to the overlay line?

I've got both cameras on the same Raspberry Pi. Do I need two separate dtoverlay lines?

My trigger isn't going to start right away when the camera boots up. Is there anything I need to change so it doesn't time out?

# Starting the camera

How do I actually enable external trigger mode on the GS camera?

Do I start the Pico code first or the camera first?

I'm losing frames even though the trigger is working fine. What's causing that?

Why do I need to set a fixed shutter value when running rpicam-apps, even though the external trigger is controlling the exposure?

If I don't set the gain manually, will I drop frames?

# Whole Document

What even is external triggering on a camera? Why would I want to use it?

I've got two Global Shutter cameras and I want them to capture frames at exactly the same time. How do I do that?

Is this external trigger thing something a total beginner can set up, or does it need soldering and stuff?

What hardware do I need to get external triggering working on the GS camera?
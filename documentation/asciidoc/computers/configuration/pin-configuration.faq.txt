# Change the Default Pin Configuration

What's the deal with custom pin configurations being deprecated? Does that mean I shouldn't bother with dt-blob anymore?

# Device Pins During Boot Sequence

My peripheral isn't responding right when the Pi boots up. Could it be a GPIO pin thing during startup?

So the GPIO pins change state multiple times during boot? Like, what order does all that happen in?

I've got something connected to a GPIO pin and it's acting weird on startup. What should I do to fix that?

What's the difference between a power-on reset and a soft reset for the GPIO pins?

My attached device needs a specific pin state right away on boot but the Pi takes a second to set things up. What are my options?

# Provide a Custom Device Tree Blob

How do I turn a `.dts` file into a `.dtb` file? Do I need a special tool for that?

I've got a Device Tree blob and I want to look at what's inside it. Can I convert it back to something readable?

What do I need to install to compile a Device Tree source file?

# Sections of the `dt-blob`

What's the `dt-blob.bin` file actually for? Is it used by Linux or something else?

I've got a Compute Module 4 — is there a specific section in the dt-blob I need to use for it?

What's the difference between `pins_rev1` and `pins_rev2`? Why are there separate sections for different Pi models?

# `pin_config`

So if I don't specifically set up a pin in `pin_config`, what happens to it? Is there some kind of default?

# `pin@pinname`

What settings can I actually configure for an individual GPIO pin in the Device Tree blob?

I want to set a pin to be pulled up on boot. How do I do that in the Device Tree source?

What drive strength values are valid for GPIO pins in the dt-blob?

# `pin_defines`

I want to move the HDMI hotplug pin to a different GPIO. Can I actually do that, and where do I set it?

What's `pin_defines` for? How's it different from `pin_config`?

# Clock Configuration

Can I change clock frequencies through the Device Tree blob? Like, is that something that actually works reliably?

I need to give an audio codec a specific clock frequency. Can the Pi do that through the Device Tree?

How complicated is it to set up custom clock routing on a Raspberry Pi? I don't really know where to start.

# Sample Device Tree Source File

Is there a sample Device Tree file I can look at to understand the format before writing my own?

# Whole Document

I've never touched Device Tree files before. What's the basic idea of what they do on a Raspberry Pi?

I want to customize my GPIO pin setup at boot time. What's the recommended way to do that now that dt-blob is kind of deprecated?

What files are involved in controlling GPIO pin states during the Pi's boot process?
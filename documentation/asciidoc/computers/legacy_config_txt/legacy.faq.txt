# Legacy options

- I'm running an older version of Raspberry Pi OS and someone told me to edit config.txt — are all those options still valid?
- I'm doing bare-metal development on my Pi. Are there config.txt settings I can use even if they're not in the main docs anymore?
- Some of these config.txt options I found online don't seem to do anything. Could they be legacy settings that got dropped?
- I'm using an older Pi OS, not Bookworm, and I want to tweak some graphics settings in config.txt. Where do I even start?
- What does it mean when a config.txt option is called "legacy"? Does that mean I shouldn't use it?
- I found a guide online that mentions some config.txt settings but I can't find them in the official docs anymore. Did they get removed or something?
- I'm not on the latest Raspberry Pi OS — should I worry about using these old config.txt options or are they safe?
- Are the firmware graphics driver settings in config.txt still usable or did they get replaced with something else?

# Whole Document

- I'm on an older Raspberry Pi OS and I'm not sure which config.txt settings actually work anymore. How do I know what's still supported?
- Someone mentioned there are deprecated config.txt options that still work on older systems. Where can I find info on those?
- I'm doing bare-metal Pi stuff and I need all the config.txt options I can get, even the unsupported ones. Is there a list somewhere?
- What's the difference between a supported config.txt option and a legacy one? Do legacy ones still actually work?
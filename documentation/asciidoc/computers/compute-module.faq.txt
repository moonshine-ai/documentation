# Whole Document

- What's the difference between a Compute Module and a regular Raspberry Pi?
- I'm trying to decide between a Compute Module and a standard Pi. Which one should I get?
- What's a CMIO board and do I need one?
- I'm building an embedded project. Should I be using a Compute Module instead of a regular Pi?
- What Compute Modules are still being made and supported?
- I've got a CM4 but I want to upgrade to a CM5. Are they compatible with each other?
- What does the Lite version of a Compute Module mean?

# Compute Modules

- So what's the actual difference between all the Compute Module versions?
- Does the CM5 have Wi-Fi built in?
- I want to use a Compute Module but I need wireless. Which ones have Wi-Fi?
- What's the most RAM I can get on a CM5?
- Can I still buy a CM3? I thought I saw it was being discontinued.
- What form factor does the CM5 use? Is it the same as the older ones?
- I have a board designed for the old DDR2 SODIMM form factor. Can I drop a CM4 in there?
- What's the difference between CM4 and CM4S?
- Why did they switch away from the SODIMM form factor on CM4?
- My project needs to work in really cold conditions, like minus 40 degrees. Is there a Compute Module that handles that?
- How much storage does the CM1 have? Can I upgrade it?

# IO Boards

- What does an IO board actually do for a Compute Module?
- Can I use a CM5 with the CM4 IO board?
- I've got an old CMIO board lying around. Will a CM4 work with it?
- What's the power difference between the CM5IO and CM4IO boards?
- Does the CM5IO have PCIe? I want to attach an NVMe SSD.
- Can I plug a regular HAT into a Compute Module IO board?
- Does the CM4IO board support PoE?
- What USB ports does the CM5IO have?
- I want to use two cameras at the same time. Which IO boards support that?
- My project only needs a couple of connectors. Do I have to use the full IO board?
- Can I attach an M.2 SSD to the CM5IO?

# CM5 and CM5IO accessories

- Is there an official case for the CM5IO?
- Does the CM5 case come with a fan?
- Can I add an external antenna to a CM5 to improve Wi-Fi?
- What's the CM5 cooler and do I actually need it?
- My CM5 is running hot. What cooling options do I have?
- Does the antenna work with both CM4 and CM5?

## CM5IO Case

- How big is the CM5IO case?
- I've got the original version of the CM5IO case. Can I fit a cooler inside without removing the fan?
- What's the difference between version 1 and version 2 of the CM5IO case?
- Can I run a camera cable out of the CM5IO case?
- How do I put my CM5IO board into the case? What order do I do things?
- I want to fit an M.2 SSD inside the CM5IO case. Is there room for it?
- How do I get the SD card out of the case? Do I just pull it?

## Antenna (CM4 and CM5)

- How long is the antenna cable?
- Does the antenna support both 2.4 and 5 gigahertz Wi-Fi?
- After I plug in the antenna, do I need to change anything in the software to use it?
- How do I actually attach the antenna when it's inside the case?

## CM5 Cooler

- How do I attach the cooler to my CM5?
- Do I need screws to attach the cooler or does it just stick?
- What's the cooler made of?

# Flash an image to a Compute Module

- How do I get an OS onto a Compute Module? It doesn't have an SD card slot.
- What do I need to flash an image to a CM5?
- Can I use a Mac to flash a Compute Module?
- I'm using Windows. How do I flash my Compute Module?
- What's rpiboot and why do I need it?
- After I've flashed the eMMC, how do I get the Compute Module to actually boot from it?
- Can I use Raspberry Pi Imager to flash a Compute Module?
- My computer isn't recognising the Compute Module when I plug it in. What do I do?
- I'm having trouble booting my CM3. Is there a known issue with that?
- What jumper do I need to set before flashing? I keep forgetting.
- What's the difference between flashing a Lite Compute Module versus one with eMMC?

# Compute Module EEPROM bootloader

- What's different about the bootloader on CM4 and newer compared to older ones?
- How do I update the bootloader on a CM4?
- I want my Compute Module to boot from the network. How do I set that up?
- Can I lock the bootloader so nobody can change it? Like for a deployed product?
- What's the BOOT_ORDER setting and how do I change it?
- Is it risky to update the EEPROM on a Compute Module?
- I want it to try USB first and then fall back to eMMC. What BOOT_ORDER value do I use?

# Wire peripherals

- I'm designing my own carrier board for a CM5. What do I need to provide at minimum?
- Which GPIO banks on the Compute Module are safe to use? Are any off limits?
- What's a dt-blob.bin and do I need to worry about it?
- How do Device Tree overlays work on a Compute Module?
- I've added an overlay to config.txt but I'm not sure if it's working. How do I check?
- What's the difference between a full Device Tree and an overlay? Which should I use?
- How do I wire an I2C device to the Compute Module IO board?
- I've wired up an SPI Ethernet chip. What do I put in config.txt to make it work?
- How do I debug Device Tree problems on my Compute Module?
- Can I use pinctrl to check what my GPIO pins are doing?

# Attach a Camera Module

- How do I connect a camera to a Compute Module?
- I've got a CM4 and a Camera Module v3. What do I need to do to get it working?
- Can I use two cameras at the same time on a Compute Module?
- Do I need to add jumper wires on the IO board to use a camera?
- I've connected my camera but it's not showing up. How do I check if it's detected?
- How do I tell the system which camera to use if I have two connected?
- How do I take a photo from the command line on a Compute Module?
- How do I record a video with the camera on a Compute Module?
- What's the difference between setting up a camera on a CM5 versus a CM1 or CM3?
- My camera driver isn't loading. Do I need to change something in config.txt?

# Attaching the Touch Display LCD panel

- Can I connect the official Raspberry Pi touchscreen to a Compute Module?
- What do I add to config.txt to enable the 7-inch touchscreen on a CM?
- I want to use the display but disable the touch part. How do I do that?
- How do I connect the display to DISP0 instead of DISP1?
- Can I just completely ignore the display even if it's plugged in?

# Attaching the Touch Display 2 LCD panel

- I've got the newer Touch Display 2. Is the setup different from the original?
- What overlay do I use for the 5-inch version of Touch Display 2 on a Compute Module?
- How do I use Touch Display 2 on the DISP0 port instead of DISP1?
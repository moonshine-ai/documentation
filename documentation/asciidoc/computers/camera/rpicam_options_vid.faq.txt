# Video options

What do these video options actually do, and how do I use them with my camera commands?

Do I need two dashes before every option, or just some of them?

# `quality`

I'm recording in MJPEG and my file sizes are huge. Is there a way to lower the quality to make them smaller?

What's the default quality level for MJPEG, and is it good enough for most uses?

The quality option goes from 1 to 100 — does that work for H.264 too or just MJPEG?

# `bitrate`

How do I control how big my H.264 video files end up being?

I'm recording at 1080p and the files are way too large. Can I set a specific bitrate to keep them smaller?

What's the shorthand flag for bitrate, I don't want to type the whole thing out every time?

# `intra`

What's an iframe and why would I want to change how often they appear?

The default intra frame frequency is 60 — should I change that, and what does it actually affect?

# `profile`

What H.264 profiles are available and how do I pick one?

I've heard about baseline and high profiles for H.264 but don't know which one to use on my Pi. What are the options?

# `level`

What does the H.264 level setting actually do, and which value should I pick?

# `codec`

What video formats can I record in, like what are all the options?

I want to record uncompressed video — is there a way to do that?

What's the default codec when I record video, do I need to specify it every time?

I've heard about libav — what is it and when would I use it instead of H.264?

# `save-pts`

I recorded a video and now I want to put it in an MKV container. How do I do that?

What's the save-pts option for, and how do I actually use it to get a proper video file?

I'm on a Pi 5 and save-pts doesn't seem to work. What should I use instead?

# `keypress`

Can I pause and resume my recording without stopping the whole program?

How do I stop recording with the keypress option — do I just hit Enter or is there something else?

# `signal`

What's the difference between using keypress and signal to control recording?

How do I toggle recording on and off using signals instead of the keyboard?

# `initial`

I want my recording to start paused so I can choose when to begin. How do I do that?

What's the default state when I start recording — does it start recording straight away?

# `split`

When I toggle recording on and off, all my footage ends up in one file. Can I make it save separate files each time?

Does split work on its own or do I need to combine it with something else to avoid overwriting my files?

# `segment`

I want to automatically split my recording into chunks every few minutes. Can I do that?

I'm trying to do something like burst photo capture with video — can I save each frame as its own file?

How do I make the filenames different for each segment so they don't overwrite each other?

# `circular`

What's a circular buffer and how does it work for video recording?

I want to keep recording in a loop and only save the footage when I quit. Is there an option for that?

Can I change how much memory the circular buffer uses?

# `inline`

What does the inline option do, and should I use it when I'm streaming video?

I'm splitting my video into segments and some of them won't play properly from the start. Is there a fix for that?

# `listen`

I want to stream video over the network from my Pi. What option do I need to make it wait for a client to connect first?

# `frames`

Can I record exactly 200 frames and then have it stop automatically?

If I set a specific number of frames to record, does that override the timeout setting?

# `framerate`

How do I lock my recording to a specific framerate like 30fps?

# `low-latency`

I'm streaming video in real time from a Pi 5 and there's too much delay. Is there a way to reduce latency?

What's the trade-off if I use the low-latency option on Pi 5?

# `sync`

I've got multiple cameras and I want them all capturing at the same moment. How do I do that?

What's the difference between client and server mode for camera synchronisation?

# Whole Document

What video recording options are there for rpicam-vid, I'm not sure where to start?

I'm new to recording video on the Pi — what format should I use, H.264 or MJPEG?

Do all these video options work on every Pi model, or are some of them only for Pi 5?

I keep typing out long option names — is there a quicker way to write most of these commands?
# Whole Document

- I'm trying to use rpicam-detect but I don't really know what options are available for it. Where do I even start?
- Are these detection settings just for rpicam-detect or do they work with the other rpicam tools too?
- How do I actually pass one of these options when I run the command? Do I just type the name or is there something I need to add?

# Detection options

- I want rpicam-detect to look for a specific object. How do I tell it what to look for?
- So the object names come from a label file. Does that mean I need to provide my own labels or does it come with some already?
- What's the `gap` option for? Like why would I want to wait between captures?
- I don't want my Pi taking a photo every single frame when it spots something. Can I slow it down a bit?
- How many frames should I set the gap to? Is there a recommended number or do I just pick whatever?
- Wait, so if I want to detect cats, I just type `--object cat` or something like that? Is it that simple?
- Do I need quotes around the object name if it's just one word, or is that only if there's a space in it?
- I've got a label file with my custom objects. How do I point rpicam-detect at it?
- What happens if I don't set the gap option at all? Does it just capture every single frame it detects something?
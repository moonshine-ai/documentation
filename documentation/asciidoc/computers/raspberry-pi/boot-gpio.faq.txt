# Whole Document

- What even is GPIO boot mode and do I need it for my Raspberry Pi?
- My Pi 3B won't boot after I messed with some settings. Could GPIO boot mode be the problem?
- Which Raspberry Pi models actually support GPIO boot mode? I've got an older one and I'm not sure if this applies to me.
- I want to choose how my Pi boots using some hardware I've wired up. Is that actually possible?
- Once I set this GPIO boot mode stuff, can I undo it later if I change my mind?

# GPIO boot mode

- How do I enable GPIO boot mode on my Raspberry Pi 3B?
- I added that line to config.txt but now my Pi won't boot at all. What did I do wrong?
- Wait, so once I set these OTP bits they're permanent? Like I can never undo them?
- Which GPIO bank should I use? I don't have a Compute Module, just a regular Pi 3B.
- I set up GPIO boot mode and now my Pi is just dead. How do I get it to boot again?
- What's the difference between Bank 1 and Bank 2 for GPIO boot mode?
- I used Bank 1 first and now I'm wondering if I can switch to Bank 2. Is that possible?
- Can I still use those GPIO pins for other stuff after the Pi has booted, or are they locked out completely?

# Pin assignments

- Which GPIO pin do I pull up to boot from USB on my Pi 3B+?
- I've got a regular Pi 3B and I want to boot from SD using GPIO boot mode. Which pin controls that?
- What's the difference between USB device boot and USB host boot in the pin table?
- I see NAND and SPI listed but it says no Linux support. Does that mean I just can't use those at all?
- What order does the Pi try the different boot modes when GPIO boot mode is set up?
- I've got a Pi 3B+ and I'm confused about which pin numbers to use because I've seen two different tables. Which one applies to me?

# Boot flow

- What's the difference between SD0 and SD1 on the Raspberry Pi?
- I've got a Compute Module Lite and I'm trying to figure out where SD0 connects to. Can you explain that?
- How strong does my pull-up resistor need to be on the GPIO boot mode pins?
- I'm trying to pull up a GPIO line for boot mode. What resistance should I use so I don't kill the Pi or drain too much power?
- What's SD1 used for on Pi models that have built-in WiFi?